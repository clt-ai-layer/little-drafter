# Sub-Agent Plan - Phase 7: Task 5 (Achievements Implementation)

Implement the `AchievementProjection` to track completed drawings and award stars/badges using Emmett's MongoDB inline projections.

## User Review Required

> [!IMPORTANT]
> The projection tracks achievements per user. I am assuming the `user_id` is available in the event metadata or stream ID, as the `DraftCompleted` event data itself does not contain a `userId`. I will use the `metadata.userId` if available, otherwise I'll fallback to a placeholder or assume the stream ID can be mapped to a user.

## Proposed Changes

### Achievement Bounded Context
#### [NEW] AchievementReadModel.ts
- Define `AchievementReadModel` schema with `completedDrawings` (number) and `stars` (number).
- Location: `packages/backend/src/Achievement/Domain/ReadModels/AchievementReadModel.ts`

#### [NEW] AchievementProjection.ts
- Implement using `defineEntityReadModel` (wrapper for Emmett's `mongoDBInlineProjection`).
- Listen for `DraftCompleted`.
- Increment `completedDrawings` and update `stars` (1 star per 5 drawings).
- Location: `packages/backend/src/Achievement/Projections/AchievementProjection.ts`

### Infrastructure
#### [NEW] projections.ts (or update existing)
- Create `packages/backend/src/projections.ts` to register all projections if it doesn't exist.
- Note: If I find where `EntityCommandBus` is instantiated, I will ensure it's added there.

### Tests
#### [NEW] AchievementProjection.test.ts
- Use `MongoDBInlineProjectionSpec` (or equivalent pattern) to verify projection logic.
- Location: `packages/backend/test/Achievement/Projections/AchievementProjection.test.ts`

## Verification Plan
- [ ] Run tests for `AchievementProjection` using `npx jest`.
- [ ] Verify `completedDrawings` increments on `DraftCompleted`.
- [ ] Verify `stars` are awarded correctly.
