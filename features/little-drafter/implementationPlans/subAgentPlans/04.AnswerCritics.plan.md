# Plan: Phase 2B — Answer Critics (Auto)

Sub-agent context: Phase 2B (Answer Critics (Auto)) for little-drafter.

## 1. Analysis of Inputs
- **Response-01-little-drafter.md**: Contains 8 Q&A pairs covering UI/UX, Domain/State, Content/Assets.
- **DocRouter Scan Results**: Confirms `es-kit` (Emmett), Vertical Slice, MongoDB, and "Paper Drawing" constraint.
- **DDD-vs-Emmett.md**: Key reference for mapping traditional DDD patterns (from `core.md`) to the actual Emmett framework used in `little-drafter`.
- **Core Rules**: Bounded contexts, enum conventions, Result pattern (vs Emmett throws), `@SerializableType` (vs Emmett plain types).

## 2. Key Findings & Critique Strategy
- **Framework Mismatch**: `Response-01` uses generic "Aggregate" terms. Need to align with Emmett `decide`/`evolve` terminology.
- **Rule Conflict**: `core.md` (Result pattern) vs `Emmett` (Throw pattern). `little-drafter` is built on `es-kit` (Emmett), so I will prioritize Emmett patterns while noting the conflict.
- **Enum Convention**: `InProgress` and `Completed` in Q5 need explicit lowercase string values (`in-progress`, `completed`) per `core.md`.
- **Event Naming**: `DrawingCompleted` (Q6) should be `DraftCompleted` to match the aggregate `Draft`.
- **Logic Clarity**: Clarify that the "Step Definition" (Q7) belongs to the Template/Content domain, while the `Draft` only tracks progress (`CurrentStepIndex`).

## 3. Output Generation
- **Critic File**: `implementationPlans/Responses/Critics/Critic-01-little-drafter.md`
    - Severity: Medium/Low issues primarily around naming and framework alignment.
- **Enriched Response**: `implementationPlans/Responses/Response-01-little-drafter.md`
    - Add `## Critic Review Notes`.
    - Apply lowercase enum values.
    - Suggest Emmett-specific terminology.

## 4. Step List
1.  **Create Critics Directory**: Ensure `c:/work/Crafts/little-drafter/features/little-drafter/implementationPlans/Responses/Critics/` exists.
2.  **Generate Critic File**: Write `Critic-01-little-drafter.md`.
3.  **Update Response File**: Edit `Response-01-little-drafter.md` in-place.
4.  **Final Report**: Summarize changes for the parent.

## Report to parent
1. **Plan Coverage**: The plan covers a full critique of the single response file, focusing on alignment with the Emmett framework (`es-kit`) and project-wide enum conventions.
2. **Open Questions**:
    - **Rule Precedence**: There is a conflict between `core.md` (traditional `Result` pattern) and the actual implementation framework `es-kit` (which uses Emmett's `throw` pattern). I am assuming `es-kit` patterns take precedence for this specific "craft" project.
3. **Relay to User**: No blocking questions. The parent should proceed with the implementation after my updates.
